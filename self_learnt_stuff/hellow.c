#include <stdio.h>

int main() { // The main function is the entry point of a C program, where execution starts
    printf("Hello, world!\n");
    return 0; // The return statement indicates that the program ended successfully, returning 0 to the operating system
}
